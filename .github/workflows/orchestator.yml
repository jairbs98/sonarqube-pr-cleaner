name: App Orchestrator

on:
  push:
    branches: [ "main" ]

jobs:
  build-action:
    uses: jairbs98/devops-workflows/.github/workflows/controller.yml@main
    with:
      app_name: "sonarqube-pr-cleaner"
      app_type: "github-action-docker"
      deploy_type: "none"
    secrets: inherit