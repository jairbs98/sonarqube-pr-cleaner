name: App Orchestrator

on:
    push:
        branches: ["main"]

permissions:
    contents: write
    packages: write
    deployments: write

jobs:
    security:
        uses: jairbs98/devops-workflows/.github/workflows/task-security.yml@main
        with:
            app_name: "sonarqube-pr-cleaner"
            app_type: "github-action-node"
            app_version: "1.0.1"
        secrets: inherit
