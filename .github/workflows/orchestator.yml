name: App Orchestrator

on:
    push:
        branches: ["main"]

permissions:
    contents: write
    packages: write
    deployments: write

jobs:
    devsecops:
        uses: jairbs98/devops-workflows/.github/workflows/controller.yml@main
        with:
            app_name: "sonarqube-pr-cleaner"
            app_type: "github-action-node"
            deploy_type: "none"
        secrets: inherit
